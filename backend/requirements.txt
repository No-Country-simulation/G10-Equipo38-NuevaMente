# Política de dependencias del backend (decisiones_proyecto.md §12.3 y §14.3):
# - Versiones exactas con "==" fijadas por cambio explícito; nunca rangos ni floating.
# - Cada adición llega con su issue correspondiente y su verificación en CI.
#
# pydantic (issue #03): motor de validación de TODOS los contratos compartidos
# (backend/app/schemas/). La versión 2.x es decisión de §7.2: usa enums, límites,
# validadores semánticos y unión discriminada; el extra="forbid" rechaza campos
# desconocidos, que es lo que impide que un contrato crezca en silencio.
pydantic==2.13.5
#
# fastapi (issue #07): framework del servicio backend (§7). Expone /api/health,
# los endpoints futuros (#9, #19, #31...) y el OpenAPI vivo en /docs.
fastapi==0.141.1
# uvicorn (issue #07): servidor ASGI que ejecuta la app FastAPI. Es el proceso
# que corre en el contenedor del backend (Dockerfile, §14).
uvicorn==0.53.0
# pydantic-settings (issue #07): lee la configuración desde variables de
# entorno/.env con los tipos y validaciones de app/config.py (Apéndice A).
pydantic-settings==2.15.0
# pypdf (issue #11): extraccion de texto y metadatos de pagina de los PDF en
# el parser (app/core/rag/parser.py). Misma version que usa la suite en
# requirements-dev (verificacion y fixture de PDF cifrado).
pypdf==6.19.0
# BPE local de referencia para chunking; vocabulario versionado con el código.
tiktoken==0.12.0
